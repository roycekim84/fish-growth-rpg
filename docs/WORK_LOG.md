# 작업 현황

## 현재 상태

- 현재 마일스톤: M14 — Deep Sea Ecology
- 완료 체크포인트: 물고기 애니메이션 + 해저 환경 + 공통 픽셀 UI + 전투 감각 + 탐험 중심 전환 + 첫 지역 발견 + 퀘스트 + 종 능력 + 보스 관문 + 심해 전환 + 지역별 개체군
- 마지막 갱신: 2026-07-25

## 완료

- 원본 게임 아이디어를 1차 프로토타입 명세로 구조화
- Flutter + Flame 기술 방향 확정
- Android/iOS 출시 및 Web 테스트 목표 반영
- 전체 픽셀아트 방향과 에셋 제작 계획 작성
- 구현 아키텍처와 테스트 전략 작성
- M0~M9 로드맵 작성
- 결정 기록 형식 도입
- Git 저장소 초기화
- 앱 식별자 `com.roycekim.fishgrowthrpg` 확정
- Flutter Android/iOS/Web 프로젝트 생성
- Flame 1.37.0 설치 및 잠금
- 세로 화면 고정과 360×640 논리 해상도 카메라 구성
- Flame World와 임시 픽셀 물고기/배경 구성
- Flutter HUD 오버레이 구성
- 초기 물고기 3종 JSON과 로더 구현
- 단위 및 위젯 테스트 2개 작성
- 정적 분석, 테스트, Web release build 통과
- Android debug APK 빌드 통과
- 드래그 앵커 기반 입력과 화면 입력 표시 구현
- 가속, 관성, 감속 및 부스터 이동 컨트롤러 구현
- 플레이어 좌우 반전과 부드러운 수직 회전 구현
- 1280×1700 필드 경계와 바깥 방향 속도 제거 구현
- 부드러운 플레이어 추적 카메라 연결
- 움직이는 거품과 카메라 공간 수중 빛 레이어 구현
- Flutter 부스터 버튼 입력 및 눌림 피드백 구현
- 이동·경계·부스터 테스트를 포함해 전체 7개 테스트 통과
- 390×844 웹 렌더링과 마우스 드래그 확인, 런타임 오류 없음
- M2 Web release 및 Android debug APK 빌드 통과
- GitHub Pages Web alpha 자동 검증·배포 워크플로 추가
- Web alpha 배포 및 장애 대응 문서 추가
- 선택형 반자동 사냥 규칙과 M4.5 실험 마일스톤 추가
- SmallFish, PufferFish, HunterFish 데이터 기반 NPC 컴포넌트 구현
- Wander/Flee/Chase 상태 머신과 종별 공격 성향 구현
- 경계 회피와 부드러운 NPC 이동·회전 구현
- 플레이어 280, NPC 28 최소 거리 안전 스폰 구현
- 종별 30/10/5, 총 45개체 유지 및 재생성 시스템 구현
- 거리 기반 0.15/0.6초 AI 판단 주기 최적화
- NPC 상태와 개체 수 임시 디버그 피드백 추가
- NPC AI 및 안전 스폰 테스트를 포함해 전체 14개 테스트 통과
- 390×844 웹에서 실제 추격·도망과 NPC 45/45 확인
- M3 Web release 및 Android debug APK 빌드 통과
- 플레이어와 NPC 원형 충돌 영역 및 충돌 시스템 구현
- 1.15/0.9 Size 기준 즉시 포식·상호 전투·위험 판정 구현
- 접촉 즉시 및 0.75초 간격 물기 피해 구현
- NPC HP, 사망 상태, 개체별 중복 포식 방지 구현
- 플레이어 HP, 안전 위치 부활, 1.5초 부활 무적 구현
- 초록/노랑/빨강 위험도 링과 HP 바, 타격·포식 픽셀 피드백 구현
- EAT/KO 임시 HUD 및 전투 메시지 구현
- 경계값과 실제 충돌·포식·재스폰을 포함해 전체 19개 테스트 통과
- 390×844 웹에서 HP HUD, 위험도 링, NPC 45/45 확인 및 런타임 오류 없음
- M4 Web release 및 Android debug APK 빌드 통과
- AUTO ON/OFF 픽셀 HUD와 SEARCH/HUNT/중단 사유 표시 구현
- 즉시 포식 가능한 최근접 초록 대상만 고르는 순수 Dart 규칙 구현
- 수동 기본 속도 75% 자동 조향과 0.15초 대상 재탐색 구현
- 150 거리 위험 대상 및 HP 35% 이하 안전 중단 구현
- 위험·저체력 중단 시 자동 추진 관성 제거 구현
- 수동 드래그, 부스터, 플레이어 KO 시 AUTO 즉시 해제 구현
- 자동 부스터·회복·종변화 및 백그라운드 진행을 추가하지 않음
- 반자동 규칙·시스템·이동·HUD 테스트를 포함해 전체 25개 테스트 통과
- 390×844 웹에서 `AUTO ON / HUNT` 이동과 안전 중단 확인, 런타임 오류 없음
- M4.5 Web release 및 Android debug APK 빌드 통과
- 순수 Dart `PlayerProgress`에 레벨, EXP, Fullness와 종별 포식 카운트 구현
- 초기 Fullness 50/100과 실제 보상 상한 처리 구현
- 포식 시 EXP·Fullness·종별 카운트 보상 연결
- `20 + Level * 10` 요구량과 한 보상 다중 레벨업 구현
- 레벨당 MaxHP +5, STR +1, Size +0.03, Weight +0.05 적용
- 레벨업 시 MaxHP 증가량만큼 현재 HP 증가 및 실제 충돌·외형 크기 반영
- 1.5초 정지, 전투 후 1초 제한, Fullness 초당 5, HP 초당 8 회복 구현
- 남은 Fullness와 HP 부족량에 비례하는 프레임 독립 회복 구현
- 숫자가 표시되는 실제 HP/FULL/EXP HUD와 STR/SIZE 디버그 정보 구현
- 회복 초록 펄스, RECOVER 상태와 LEVEL UP 피드백 구현
- 성장·보상·회복 테스트를 포함해 전체 31개 테스트 통과
- 390×844 웹에서 수치 HUD, 피해, 부활 갱신 확인 및 런타임 오류 없음
- M5 Web release 및 Android debug APK 빌드 통과
- 시작 종과 해금·발견 종 목록을 포함하는 `PlayerProgress` 종 상태 구현
- 실제 100번째 포식에서 한 번만 종변화가 해금되는 이벤트 구현
- 시작 종을 항상 해금 상태로 유지하고 미해금 NPC 종 변경을 차단
- SmallFish/PufferFish/HunterFish의 MaxHP·STR·SPD·Size·Weight 배율 데이터 추가
- 종변화 시 플레이어 외형·이동 속도·전투 스탯·충돌 크기 갱신 구현
- 종변화 전후 HP 비율 유지와 전투 직후 1초 변경 제한 구현
- 종별 포식 수, 남은 수, 해금 상태, 기본 스탯과 설명을 표시하는 도감 구현
- 시작 종과 3개 NPC 종을 비교·선택하는 종변화 화면 및 잠금 피드백 구현
- BOOK/CHANGE HUD 버튼과 모달 오버레이 동안 월드 일시 정지 구현
- 해금·배율·전투 제한·오버레이 테스트를 포함해 전체 36개 테스트 통과
- 390×844 웹에서 도감, 종변화, 미해금 상태를 확인하고 오버플로 없음
- M6 Web release 및 Android debug APK 빌드 통과
- Flutter 공식 `shared_preferences` 2.5.5와 `SharedPreferencesAsync` 로컬 저장 연결
- 플러그인과 분리된 `PlayerSaveData` schema version 1 및 저장소 인터페이스 구현
- 레벨, EXP, HP, Fullness, 현재 종, 해금·발견 종, 포식 카운트 직렬화 구현
- 모든 저장 시간의 UTC ISO-8601 기록 구현
- 손상 JSON 자동 제거와 미래 schema 파일 비덮어쓰기 정책 구현
- 진행 상태 변경 700ms 디바운스 저장과 종변화 즉시 저장 구현
- inactive/paused/detached 앱 수명주기 저장 어댑터 구현
- LOAD/LOCAL/PENDING/SAVING/SAVED/RECOVERED/SAVE ERR HUD 상태 표시 구현
- 저장 codec·손상 복구·게임 복원·수명주기 테스트를 포함해 전체 41개 테스트 통과
- 390×844 Web에서 저장 상태 UI와 런타임 오류 없음 확인
- Android API 36 에뮬레이터에서 HP 26 저장 후 강제 종료·재실행 복원 확인
- M7 Web release 및 Android debug APK 빌드 통과
- 시작 종·SmallFish·PufferFish·HunterFish 픽셀 모델 시트 생성 및 원본 보존
- 제한 팔레트와 투명 배경으로 정리한 종별 4프레임 runtime strip 제작
- nearest-neighbor 필터와 0.14초 프레임 루프를 플레이어·NPC에 연결
- 기존 이동 방향 기반 좌우 반전과 종변화 즉시 외형 갱신 연결
- 이미지 manifest와 재현 가능한 `tool/build_fish_sprite_strips.py` 파이프라인 추가
- 중간 가공본은 `art_source/processed`, 최종 경량 스트립만 앱 번들에 포함
- 430×932 Web에서 네 종 표시, 애니메이션, 위험도 링 및 런타임 오류 없음 확인
- 스프라이트 계약·프레임 루프 테스트를 추가해 전체 43개 테스트 통과
- M8 물고기 스프라이트 체크포인트 Web release 및 Android debug APK 빌드 통과
- 저대비 256×256 픽셀 해저 배경 타일 제작 및 체커 임시 배경 교체
- 수초·암초·산호·조개·석상 8종을 256×128 runtime props atlas로 제작
- 환경 원본·투명화 중간본·runtime 에셋과 `tool/build_environment_assets.py` 파이프라인 분리
- 장식 12개를 결정적 위치에 배치하고 충돌 없는 배경 전용 레이어로 유지
- 상승하는 사각 거품과 부유물 파티클을 픽셀 스타일로 갱신
- 환경 에셋 크기·패키징 계약 테스트를 추가해 전체 44개 테스트 통과
- 430×932 Web에서 반복 경계·물고기 대비·장식 밀도와 런타임 오류 없음 확인
- `PixelPalette`, `PixelPanel`, `PixelButton`, 분절 게이지, 공통 헤더 UI 시스템 구현
- HUD 타이틀·상태 패널·BOOK/CHANGE/AUTO/BOOST 버튼을 이중 픽셀 프레임으로 통일
- HP·Fullness·EXP 게이지에 10칸 분절 표시와 수치 레이어 적용
- 도감과 종변화 화면의 단색 박스를 실제 종별 첫 프레임 초상화로 교체
- 도감 카드와 종변화 선택·미해금·현재 상태에 `?`, `!`, `✓`, `×`, `■`, `◆` 기호 병행
- NPC 포식 가능·상호 전투·위험 관계에 색상과 함께 `+`, `=`, `!` 마커 적용
- 기존 UI 상호작용과 전체 44개 테스트 및 `flutter analyze` 통과
- 430×932 HUD·도감·종변화와 390×844 종변화 화면에서 오버플로·런타임 오류 없음 확인
- bite 이빨, 피격 파편, 포식 확산, 레벨업 금색 상승, 종 해금 보라색 상승 파티클 구현
- 저장소에서 재생성 가능한 22.05kHz mono PCM 8비트풍 효과음 4종 제작
- `flame_audio` 2.12.2로 포식·피격·레벨업·해금·KO 이벤트를 Web/Android/iOS 오디오에 연결
- Web에서는 오디오만, Android/iOS에서는 이벤트 강도별 light/medium/heavy 햅틱을 함께 적용
- 오디오 디코딩·웹 autoplay·햅틱 미지원 시 핵심 게임 진행을 유지하는 best-effort 피드백 계층 구현
- 피드백 무음 대역과 파티클 프로필 테스트를 추가해 전체 46개 테스트 통과
- Web release와 Android debug APK 빌드 통과
- 430×932 Web에서 AUTO 입력 후 실제 피격 파편·HP 감소와 콘솔 오류 0건 확인
- Web 서버 요청에서 WAV 4종 프리로드가 모두 HTTP 200으로 완료됨을 확인
- 충돌·포식·주요 이벤트를 3개 감각 채널로 묶고 140/80/300ms 쿨다운을 적용해 동시 접촉 시 오디오·햅틱 중첩 제한
- 320×568 소형 화면에서 로드한 게임을 430×932로 실시간 리사이즈하는 HUD 회귀 테스트 추가
- runtime 에셋과 코드에서 임시 도형·placeholder가 남지 않았음을 감사하고 과거 문서 기록만 유지
- GitHub Pages 액션을 Node 24 기반 `configure-pages@v6`, `upload-pages-artifact@v5`, `deploy-pages@v5`로 갱신
- 피드백 제한과 세로 화면 회귀를 포함해 전체 48개 테스트 및 `flutter analyze` 통과
- Web release와 clean 재생성 Android debug APK 빌드 통과
- Master Spec을 `Fish Adventure RPG` 기준으로 추가
- 기존 성장/사냥 명세를 legacy prototype spec으로 표시
- 사냥, 레벨, 종변화, 도감, 반자동 사냥의 역할을 탐험 보조 시스템으로 재정의
- 신규 마일스톤을 Region/Discovery, Quest, Species Ability, Boss Gate, Adventure Alpha 순서로 재배치
- 사용자 승인 후 M9 착수
- `ocean_shallows` 지역과 5개 발견 지점을 JSON 데이터로 정의
- 지역 진입, 지점 발견, 지역 완료를 위한 discovery event bus 추가
- HUD에 현재 지역과 발견 진행도 표시
- Explorer Book에 Species/Regions 탭 분리 및 발견 지점 기록 표시
- 지역 및 지점 발견 기록을 save schema v2로 저장하고 기존 v1 저장은 자동 migration
- 조개 안내자 누리 NPC marker와 근접 TALK 상호작용 추가
- 탐험 퀘스트 `빛나는 길의 단서`와 수집 퀘스트 `작은 무리 관찰 기록` 추가
- 퀘스트 상태 inactive/active/completed와 순차 수락 규칙 구현
- 첫 탐험 퀘스트 보상으로 `small_fish` 종을 해금하는 경로 추가
- quest progress를 save schema v3에 저장하고 v1/v2 저장은 자동 migration
- 종 데이터에 ability id, 이름, 설명을 추가
- 작은 물고기의 `좁은 해류 통과`, 복어의 `산호 돌파` 능력 구현
- 능력별 게이트를 실제 플레이어 이동 제한으로 연결
- 작은 물고기만 접근 가능한 `속삭이는 해류` 발견 지점 추가
- 종변화 화면에 고유 능력 설명 표시
- 북쪽 해류 너머의 보스 아레나와 미니 보스 `해류의 수호자` 추가
- 보스 처치 시 `deep_sea` 지역 해금 및 관문 개방
- boss defeat와 unlocked region을 save schema v4에 저장하고 구버전 저장 자동 migration
- 기존 즉시 부활 구조를 유지해 보스 재도전 패널티를 최소화
- 보스 관문 진입 시 실제 `deep_sea` 지역으로 전환하고 심해 팔레트 적용
- 심해 입구 랜드마크 3개와 세 번째 퀘스트 `심해의 메아리` 추가
- 현재 지역을 save schema v5에 저장해 재시작 후에도 같은 지역에서 이어가기
- Explorer Book에 Quest 탭을 추가해 Species/Regions/Quests 통합 기록 제공
- 종별 지역 스폰 수 데이터를 추가해 심해에서는 작은 물고기 8, 복어 6, 사냥 물고기 12마리로 개체군 전환
- 심해 퀘스트 `심해의 메아리` 완료 보상으로 사냥 물고기 종변화를 해금
- 사냥 물고기의 `심해 감각`으로만 통과 가능한 메아리 해구와 네 번째 심해 발견 지점 추가
- 심해 분출구 2개를 추가해 범위 안에서 지속 피해와 바깥쪽 해류 밀어내기 적용
- 분출구 피해·밀어내기와 심해 전환 회귀 테스트를 추가해 위험 지역 이탈과 지역별 생태 전환을 검증

## 진행 중

- M14 Deep Sea Ecology 구현

## 다음 작업

- 심해 전용 신규 종 설계·구현
- 웹 알파 플레이테스트 및 심해 난이도 조정
- Android/iOS 물리 기기 M8 최종 승인은 병행 유지

## 블로커/미결정

- 플레이어 시작 종의 최종 이름과 DEX/INT 수치
- 최종 앱 이름, package/bundle ID 변경 여부
- Fullness를 Hunger로 저장 schema까지 바꿀지 여부
- 100마리 포식 해금을 완전히 제거할지, collection quest로 유지할지 여부
- iOS 빌드 환경: 설치된 CoreSimulator 1051.54.0이 Xcode 요구 버전 1051.55.0보다 낮음
- iOS simulator 서명 대상 Flutter.framework에 macOS resource fork/metadata 오류 존재
- Android API 36 에뮬레이터 실행·저장 복원은 확인했으며 실제 물리 기기 터치 검증은 후속 필요
- 자동 입력 없이 중앙에서 장시간 정지하면 Hunter가 재접근해 연속 부활할 수 있어 실기기 플레이에서 스폰·추격 밸런스 확인 필요
